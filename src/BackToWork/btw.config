# trigger combination
hotkey = Shift+Ctrl+0

# sorted descending by priority
app = gvim
app = qt creator
app = visual studio
app = emacs
app = sublime
app = atom
app = notepad++
app = code

# activate minimum 2 windows (e.g. if you have 2 monitors)
min_windows = 2